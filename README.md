# Computer_Vision_Mediapipe
The source code of CV's implementation with Mediapipe from Google

# Install dependencies
- ```pip install opencv-python```
- ```pip install mediapipe```
- ```pip install cvzone```
